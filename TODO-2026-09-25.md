# TODO — 26.09.25 beta

Working line: `26.09.25`. Last published GitHub release: `v26.09.24-a1`.
This commit is local main only. Do not push. Do not publish a GitHub release from this commit.

Plan: `docs/plans/26.09.25-beta.md`.

## Done in this commit

- [x] Clean the working tree. Dirty work from `release/26.09.21-r1` is in `git stash` (`pre-26.09.25 dirty on release/26.09.21-r1`). Unique panel and harness files are also copied under the operator scratch dir `bs-dirty-260925`. Do not drop that stash until the panel work reviews it.
- [x] Fast-forward local `main` to `v26.09.24-a1` (`e29087f`), then stamp `26.09.25`.

## Beta work

- [ ] 1. Icons. Center the menu-bar "B" horizontally and vertically. Add a real app icon, tray icon, and installer icon on macOS, Windows, and Linux. Set the icon in every package path.
- [ ] 2. GitHub release standard. Update the changelog, put current status at the top of the README, keep CI green, update docs, and update `/home/work/bridgesessions-site`.
- [ ] 3. Bridge Panel. Test it. Make it work on a PC browser and on a phone browser.
- [ ] 4. Built-in e2e. File transfer is fast. Typing is fast. Several sessions can run. Sessions do not stack. Sessions do not die by themselves. Session names come from the harness.
- [ ] 5. Bridge Panel sessions tab. When a host is selected, show live sessions that are not cron jobs and not agent jobs. Design it, then install it on the panel.
- [ ] 6. Fleet auto-update. Prove a strategy that updates peers and does not crash the host and does not loop every second.
- [ ] 7. Loop audit, then Devin. Remove every 1-second reconnect or upgrade loop. Then run `devin` CLI `swe-2` on Linux, macOS, and Windows. Windows runs on the Shadow PC through WinRM. E2e scripts stay in this repo.

## Do not do yet

- Do not roll the fleet to `26.09.24-a1` or `26.09.25`.
- The running Windows file hash is `AE2B61A6`. The published Windows file hash is `684d4149`. Trace that hash before any roll.
- Do not push `main` until a real release exists and the installer tag can be downloaded.
