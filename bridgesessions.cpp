// bridgesessions.cpp — test-compatibility stub
// Former monolith. The bs::mesh namespace lives in bs-protocol.h.
// main() lives in main.cpp (only compiled in non-test builds).
// This file exists for test compatibility — test TUs that used to
// #include "../bridgesessions.cpp" now get the protocol via this shim.

#include "bs-protocol.h"
