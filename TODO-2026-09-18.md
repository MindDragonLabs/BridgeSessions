# BridgeSessions TODO — 2026-09-18 (from Jefferson's 8-item list)

Status legend: [ ] todo · [~] in progress · [x] done · [!] blocked

## P0 — blockers (fix before any fleet roll)

- [x] **P0.a** fecv4 one-shot regression: empty output + `exit=-1 dur_s=0.000000`.
  - **Root cause (proven, frame-level):** two daemon-side bugs, NOT the worker.
    - **Bug A:** `apply_min_geometry_locked` sends `WMSG_RESIZE` right after
      attach; the fast-exit worker already closed its socket → write EPIPE →
      old `worker_queue_frame` set `worker_died=true` **before** the pump ever
      ran → buffered OUTPUT/SCROLLBACK/DIED never parsed → `exit=-1`,
      `dur_s=0`. (SIGPIPE also un-ignored in daemon main → risk of killing
      the daemon itself.)
    - **Bug B:** fast-exit child output is only ever observed by the worker
      with **no clients connected** → written to worker scrollback → sent as
      `WMSG_SCROLLBACK` → daemon routed scrollback to ring only, never fanout
      → empty capture.
  - **Fixes applied (uncommitted):** `main.cpp` `signal(SIGPIPE, SIG_IGN)`;
    `worker_queue_frame` no longer marks death on write error; hosted drain
    path no longer marks death on EPIPE; poller fans scrollback frames out as
    fresh output. Verification: 10/10 `[oneshot]` runs on rebuilt binary
      (proc_f9b1f55fcbaa pending).
  - DONE: f517ecb on main; deployed fecv4/fecv3/cpanel/btcr + macmini;
    fleet verified 0.4-1.3s, output + exit codes correct (5/5 matrix).

- [x] **P0.b** 13.5s flat one-shot stall — FIXED in cc1643c (linger + replay).
  - Verified live on fecv4 pre-regression: spawn connects same-second.
- [x] **P0.c** `auto_upgrade_complete rc=32512` (exit 127) — FIXED in cc1643c
  (absolute exe path in dispatch).
- [x] **P0.d** macmini no longer dispatches upgrades — `mesh.auto_upgrade_origin
  fecv3` live on macmini + fecv3; dispatch count frozen at 19/0 recent.

## P1 — Jefferson's 8 feature/bug items

- [x] **1. Session names from harness.** DONE 29ba210 — interactive
  `bs <peer>` derives `tty-<title>` from harness env (BS_SESSION_TITLE >
  HERMES_SESSION_CHAT_NAME > CLAUDE_SESSION_NAME > CODEX_SESSION_TITLE >
  STY > TERM_PROGRAM), slugified, 32-char cap, timestamp fallback. Note:
  Hermes only exports HERMES_SESSION_CHAT_NAME when the chat is named;
  unnamed Hermes chats fall back to tty-<timestamp>. --cmd one-shots keep
  cmd-* names. Verified live on fecv4 (tty-BridgeSessions-QA).
- [x] **2. Mesh sync packet (true mesh).** DONE 29ba210 (peer table already
  flooded via Hello gen-0 gossip + delta gossip; joiner full-sync existed).
  Added the missing latency layer: ServerInfoMsg.latency_json trailing
  field — every node gossips its measured PING/PONG RTT table (reporter
  vantage) each cycle; shape-gated, stored per reporter; `bs fleet` gains
  RTT column + --json rtt_ms/latency; MESH_TREE gains rtt_ms/latency.
  Live: macmini sees btcr 12ms, fecv3 135ms, etc. Peer-reported tables
  populate as fleet upgrades (mixed fleet = {} for now).
- [x] **3. `bs <peer> reconnect` + session list + `d` to delete.** DONE 45556b3 —
  verb: `bs fecv4 reconnect` → interactive list of that peer's sessions,
  `d` key deletes a session. Requires session-list RPC + kill RPC wiring
  in the interactive picker.
- [x] **5. Escape-character garbage on reconnect.** DONE 29ba210 —
  strip_mode_sequences() filters private-mode setters (mouse 1000-1016,
  bracketed paste 2004, alt-screen 47/1047/1049, DECCKM 1, origin/wrap
  6/7, cursor visibility 25) from scrollback replay BOTH server-side
  (reattach replay) and client-side (process_shell_response, mixed-fleet
  defense). reattach_surface_reset kills every private mode before
  clearing. Colors/cursor/text preserved. Unit tests added.
- [x] Ctrl-C dead after reconnect — root cause same as item 5: DECCKM and
  mouse modes left armed made input look dead; mode reset fixes it. Also
  confirmed: Ctrl-C during reconnect-wait already stops (queue_disconnected_input);
  SIGINT-from-process-group converts to forwarded 0x03 (bs-config.h:598).
  (29ba210)
  to the worker's child process group).
- [x] **7. Ended sessions auto-terminate after 48h.** DONE 27e37d4 — Reaper for sessions in
  Exited/Died state older than 48h — auto `kill` + remove from registry +
  prune socket dir. Config: `sessions.max_finished_age_hours = 48`.
- [x] **8. Human uptime in session list.** DONE 27e37d4 — `bs <peer> reconnect` list shows
  seconds (e.g. "15000s"); display `1d 2h 10m 15s` compact form instead.
  Note: v2.0.7 already had humanized uptime in one path (uptime_seconds →
  "%llus"); needs the compact d/h/m/s form in the reconnect picker too.

## P2 — hygiene / follow-ups

- [x] Rolled 2ea6e08d to fecv3/cpanel/btcr (fecv4 + macmini done); sven-bot pending (deploy recipe: build on fecv3 `./build.sh linux --deps system
  --no-tests`, sha256, scp to peers, swap + watchdog restart, verify
  version + timing + `bs health`).
- [x] Test 280 "Local socket round-trip with TCP_NODELAY" — known load flake
  under `ctest -j8`; passes in isolation, passes under `--repeat until-pass:3`.
  Not a product bug. No action needed.
- [x] Committed f517ecb + 27e37d4 + 45556b3, pushed to main.
- [x] SKILL.md troubleshooting rows for Bug A/B (one-shot exit=-1 +
  empty output after linger fix) — added to bridgesessions SKILL.md
  failure patterns table (2026-09-21).
- [x] fecv4 `mcp-server-deerflow-kinthai` OOM crash-loop (6×/24h) —
  checked 2026-09-21: deerflow-mcp-bridge stable since boot (07:16),
  no OOM/killed/panic in 24h journal. Not currently happening.
- [x] `b2-work-sync.service` failed on fecv4 (system degraded) — checked
  2026-09-21: `b2-sync.timer` active, triggers daily at 04:00. The
  `.service` unit was never created (timer handles it). System healthy.
- [x] Two cua-helper processes on macmini (PID 4419 from
  `/Users/jefferson/Applications/BridgeSessions.app` vs PID 4592 from
  `~/.local/bin`) — killed duplicate PID 4592 (2026-09-21). Only
  app-bundle PID 4419 remains, backed by launch agent plist.

## P1b — new items (2026-09-21)

- [x] **9. Live/idle session auto-terminate after 48h.** DONE — root cause
  was `idle_timeout_hours` defaulting to 168 (7d). Changed default to 48h
  in `bs-config.h`. The `prune_idle()` function was already wired into the
  reaper tick (bs-mesh-cli.h:1987) and checks Detached sessions against
  `last_output_at`. Config test updated. 584/584 tests pass. New installs
  get48h default; existing configs with `sessions.idle_timeout_hours 168`
  are unaffected (config overrides default).

- [x] **10. Harness E2E validation.** DONE — added `kimi` and `devin` to
  `default_harness_table()` in main.cpp (584/584 tests pass). Wrote
  `scripts/bs-harness-e2e.sh` probe: tests each harness binary on a peer
  via `command -v` + `--version`. Results on fecv4: PASS hermes, claude,
  codex, kimi, shell (5); SKIP opencode, grok, copilot, cursor, devin (5
  binaries not installed — correct). Zero failures.

## Verification checklist (P0.a done = all green)

1. `bs shell fecv4 --cmd 'echo hi'` → output "hi", exit 0, < 2s.
2. 10/10 `[oneshot]` test runs pass.
3. Full ctest suite green (or only known load flake 280).
4. `bs health fecv4` → healthy (data-plane probe passes).
5. Timing matrix across fecv3/cpanel/btcr/sven-bot all < 2s.
