[Unit]
Description=bridgesessions mesh daemon
After=network.target

[Service]
Type=simple
User=bridgesessions
Group=bridgesessions
ExecStart=/usr/local/bin/bridgesessions --config-dir=%h/.bridgesessions --daemon
Restart=always
RestartSec=5
NoNewPrivileges=true

[Install]
WantedBy=multi-user.target