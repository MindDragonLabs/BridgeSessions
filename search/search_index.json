{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"BridgeSessions documentation","text":"<p>BridgeSessions is a trusted-mesh terminal, file-transfer, and desktop-automation tool in one C++23 executable.</p>"},{"location":"#start-here","title":"Start here","text":"<ul> <li>Quickstart</li> <li>Usage</li> <li>Configuration</li> <li>Security</li> </ul>"},{"location":"#understand-the-system","title":"Understand the system","text":"<ul> <li>Design</li> <li>Protocol</li> <li>Why BridgeSessions</li> </ul>"},{"location":"#platform-and-release-work","title":"Platform and release work","text":"<ul> <li>Computer-use automation</li> <li>Bridge Panel</li> <li>Building</li> <li>E2E Framework</li> <li>Release provenance</li> <li>Current audit</li> </ul>"},{"location":"E2E-FRAMEWORK/","title":"E2E Framework","text":"<p>Unit tests prove local contracts. E2E lanes prove the real mesh, desktop session, installer, and platform packaging.</p>"},{"location":"E2E-FRAMEWORK/#layers","title":"Layers","text":"Layer Scope Default L0 CTest and Python tests every change L1 local loopback daemon/client protocol changes L2 live mesh: health, shell, files, scripts release candidates L3 Windows/macOS/Linux desktop helper and tray CUA changes L4 clean-profile installer and upgrade release candidates"},{"location":"E2E-FRAMEWORK/#run","title":"Run","text":"<pre><code># Discover every healthy configured seed\nscripts/e2e-fleet-test.sh --all\n\n# Explicit sanitized peer list\nBS_E2E_PEERS=\"linux-peer,macos-peer,windows-peer\" \\\n  scripts/e2e-fleet-test.sh --json /tmp/bs-l2.json\n\n# Orchestrated desktop lanes\nBS_E2E_PEERS=\"linux-peer,macos-peer,windows-peer\" \\\n  python3 tests/e2e/runner.py --layers L2,L3 --json /tmp/bs-e2e.json\n</code></pre> <p>Pass live peer names through arguments/environment variables. Never hardcode a private fleet in the repository.</p>"},{"location":"E2E-FRAMEWORK/#desktop-prerequisites","title":"Desktop prerequisites","text":"<ul> <li>Linux: disposable VM with a logged-in desktop and display/input tools.</li> <li>Windows: interactive user session, one CUA helper, Explorer desktop.</li> <li>macOS: logged-in user, signed helper/app, Screen Recording and Accessibility grants.</li> </ul> <p>Idempotent setup/probe helpers live under <code>tests/e2e/harness/</code>. They are test infrastructure, not installation entry points.</p>"},{"location":"E2E-FRAMEWORK/#evidence","title":"Evidence","text":"<p>A complete run records:</p> <ul> <li>exact local binary version,</li> <li>peer/version matrix,</li> <li>command exit codes and output,</li> <li>file checksum results,</li> <li>screenshots/video metadata for desktop lanes,</li> <li>JSON summary,</li> <li>skipped lanes with reasons.</li> </ul> <p>E2E artifacts are local/CI outputs and must not be committed.</p>"},{"location":"E2E-FRAMEWORK/#release-gate","title":"Release gate","text":"<ol> <li>Run L0.</li> <li>Run L1/L2 for protocol or transfer changes.</li> <li>Run affected L3 platforms for CUA changes.</li> <li>Run L4 on disposable profiles for installer/upgrade changes.</li> <li>Fix product or harness failures; do not convert a required failure into a skip.</li> </ol>"},{"location":"QUICKSTART/","title":"Quickstart","text":""},{"location":"QUICKSTART/#install","title":"Install","text":"<pre><code>curl -fsSL https://raw.githubusercontent.com/MindDragonLabs/BridgeSessions/main/scripts/install.sh | bash\n# Windows PowerShell:\n# irm https://raw.githubusercontent.com/MindDragonLabs/BridgeSessions/main/scripts/install.ps1 | iex\n</code></pre> <pre><code>bs --version\nbs doctor\n</code></pre>"},{"location":"QUICKSTART/#join","title":"Join","text":"<p>On a pinned seed:</p> <pre><code>bs invite\n</code></pre> <p>On the new node, use the printed address/token immediately:</p> <pre><code>bs join &lt;seed-address&gt;:19949 &lt;token&gt; --start\nbs health &lt;peer&gt;\n</code></pre> <p>Only explicitly pinned seeds can issue accepted mesh-wide enrollments.</p>"},{"location":"QUICKSTART/#shell","title":"Shell","text":"<pre><code>bs shell &lt;peer&gt;\nbs shell &lt;peer&gt; --name agent\nbs shell &lt;peer&gt; --cmd 'hostname &amp;&amp; uname -a'\n</code></pre> <p><code>Ctrl-D</code> detaches; the remote PTY remains. Reuse <code>--name</code> to reattach.</p>"},{"location":"QUICKSTART/#files-and-scripts","title":"Files and scripts","text":"<pre><code>bs file send &lt;peer&gt; ./artifact.bin --wait\nbs file send &lt;peer&gt; ./config.toml --dest configs/config.toml --wait\nbs file recv &lt;peer&gt; received/report.md --to ./report.md --wait\nbs run-script &lt;peer&gt; ./deploy.sh\n</code></pre> <p>Success requires the final <code>OK</code> after SHA-256 verification.</p>"},{"location":"QUICKSTART/#computer-use","title":"Computer use","text":"<p>Windows/macOS need one <code>bridgesessions --cua-helper</code> in the interactive user session. macOS also needs Screen Recording and Accessibility permission.</p> <pre><code>bs cua screen &lt;peer&gt;\nbs cua capture &lt;peer&gt; -o screen.png\nbs cua click &lt;peer&gt; --x 500 --y 300\n</code></pre> <p>Capture before clicking. Continue with Usage, Configuration, and Security.</p>"},{"location":"RELEASE-PROVENANCE/","title":"Release provenance","text":"<p>Generated executables, app bundles, archives, checksums, and SBOMs are release artifacts. They are ignored by git and attached to a GitHub Release whose tag points to the reviewed source commit.</p>"},{"location":"RELEASE-PROVENANCE/#required-assets","title":"Required assets","text":"Asset Platform <code>bridgesessions-linux-x86_64</code> Linux x86_64 <code>bridgesessions-macos-arm64</code> macOS arm64 <code>bridgesessions-windows-x86_64.exe</code> Windows x86_64 <code>SHA256SUMS</code> checksum manifest <code>SBOM-binaries.json</code> CycloneDX artifact/dependency record <p>Optional source archives are generated from the exact tag with <code>git archive</code>; GitHub also publishes automatic source archives.</p>"},{"location":"RELEASE-PROVENANCE/#build-requirements","title":"Build requirements","text":"<ul> <li>clean source tree,</li> <li><code>VERSION</code> matches the intended tag,</li> <li>supported dependency lines (OpenSSL 3.5 LTS; spdlog newer than 1.15.1),</li> <li>release hardening flags enabled,</li> <li>no operator paths, private hosts, credentials, or debug symbols in public artifacts,</li> <li>macOS Developer ID signature and notarization,</li> <li>Windows PE ASLR/NX validation,</li> <li>Linux PIE/RELRO/NX/stack-protector validation.</li> </ul>"},{"location":"RELEASE-PROVENANCE/#stage-locally","title":"Stage locally","text":"<pre><code>scripts/package-release.sh --release\nscripts/release-checksums.sh\n</code></pre> <p><code>dist/</code> is local staging and is ignored by git.</p>"},{"location":"RELEASE-PROVENANCE/#verify","title":"Verify","text":"<pre><code># Source and tests\ncmake -S . -B build/release -DCMAKE_BUILD_TYPE=Release\ncmake --build build/release --parallel\nctest --test-dir build/release --output-on-failure\nbash scripts/prepublish-scan.sh\n\ngitleaks dir . --redact --no-banner\n\n# Assets\n(cd dist &amp;&amp; shasum -a 256 -c SHA256SUMS)\nfile dist/bridgesessions-*\nstrings dist/bridgesessions-linux-x86_64 | grep -E '^/home/|^/Users/' &amp;&amp; exit 1 || true\n</code></pre> <p>Run each binary on its target OS and compare <code>--version</code> with <code>VERSION</code>. Run the affected E2E layers from E2E Framework.</p>"},{"location":"RELEASE-PROVENANCE/#publish","title":"Publish","text":"<ol> <li>Commit the source-only tree.</li> <li>Push <code>main</code> and wait for Build/Test and Security workflows.</li> <li>Create or move the annotated release tag only after green CI.</li> <li>Push the tag.</li> <li>Run <code>scripts/github-release.sh</code> to create the prerelease and upload verified assets.</li> <li>Read the release back with <code>gh release view</code> and compare every remote digest/size with local files.</li> </ol> <p>The release script refuses a dirty tree, tag/HEAD mismatch, origin-tag mismatch, missing asset, or implicit replacement of an existing release.</p>"},{"location":"RELEASE-PROVENANCE/#dependency-evidence","title":"Dependency evidence","text":"<p>Record exact compiler, OS image, and dependency versions in the GitHub Release notes or SBOM. Do not copy binaries or checksum files forward from an older tag.</p>"},{"location":"bridge-panel/","title":"Bridge Panel","text":"<p>Bridge Panel is an optional local web UI for reviewing Markdown and session output produced by humans or agents.</p>"},{"location":"bridge-panel/#start","title":"Start","text":"<p>From the repository root:</p> <pre><code>python3 -m tools.bridgepanel\n</code></pre> <p>It binds <code>127.0.0.1:9770</code> by default and talks to the local BridgeSessions daemon over token-authenticated loopback IPC.</p>"},{"location":"bridge-panel/#publish-a-document","title":"Publish a document","text":"<pre><code>bs pane publish report.md \\\n  --session default \\\n  --type documents \\\n  --title 'Audit report'\n</code></pre> <p>The panel provides:</p> <ul> <li>session/document tree,</li> <li>breadcrumb navigation,</li> <li>rendered Markdown,</li> <li>raw copy,</li> <li>edit/save/cancel for writable documents,</li> <li>live session output where supported.</li> </ul> <p></p> <p></p>"},{"location":"bridge-panel/#security","title":"Security","text":"<ul> <li>Loopback is the default and recommended bind.</li> <li>Writes require a generated bearer token.</li> <li>A non-loopback bind is an explicit operator choice and must be protected by a trusted VPN/firewall and configured source controls.</li> <li>Do not expose the panel directly to the internet.</li> <li>Bridge Panel does not currently implement per-device mTLS pairing or OIDC.</li> <li>Panel files are constrained to its session/document roots; do not run it as a more privileged user than necessary.</li> </ul>"},{"location":"bridge-panel/#development","title":"Development","text":"<pre><code>python3 -m pytest tools/bridgepanel/test_bridgepanel.py -q\npython3 -m tools.bridgepanel\n</code></pre> <p>The implementation lives in <code>tools/bridgepanel/</code>. Keep launchers there; do not add one-off wrapper scripts at the repository root.</p>"},{"location":"building/","title":"Building","text":"<p>BridgeSessions requires C++23 and CMake 3.25+.</p> <p>Dependencies: OpenSSL 3, zstd, spdlog/fmt, CLI11, nlohmann-json, and Catch2 3 for tests.</p> <pre><code># macOS\nbrew install cmake ninja openssl@3 zstd fmt spdlog cli11 nlohmann-json catch2\n\ncmake -S . -B build -GNinja -DCMAKE_BUILD_TYPE=RelWithDebInfo\ncmake --build build --parallel\nctest --test-dir build --output-on-failure\n</code></pre> <p>Source-only release build:</p> <pre><code>cmake -S . -B build/release -GNinja -DCMAKE_BUILD_TYPE=Release -DBUILD_TESTING=OFF\ncmake --build build/release --target bridgesessions --parallel\n</code></pre> <p>Release builds enable stack protection, fortified libc, PIE/RELRO/NX on Linux and ASLR/NX/Control Flow Guard where supported on Windows.</p>"},{"location":"building/#sanitizers","title":"Sanitizers","text":"<pre><code>cmake -S . -B build/asan -GNinja -DCMAKE_BUILD_TYPE=Debug \\\n  -DCMAKE_CXX_FLAGS='-fsanitize=address,undefined -fno-omit-frame-pointer' \\\n  -DCMAKE_EXE_LINKER_FLAGS='-fsanitize=address,undefined'\ncmake --build build/asan --parallel 2\nctest --test-dir build/asan --output-on-failure\n</code></pre>"},{"location":"building/#release-platforms","title":"Release platforms","text":"<ul> <li>Linux x86_64: <code>scripts/Dockerfile.static-linux</code> (OpenSSL 3.5 LTS, supported dependencies)</li> <li>Windows x86_64: <code>scripts/build-windows-mingw.sh</code> or native MSVC/vcpkg</li> <li>macOS arm64: native static dependency build, Developer ID sign, notarize</li> </ul> <p><code>dist/</code> is ignored local staging. Generated artifacts belong in GitHub Releases, not commits. See Release provenance.</p>"},{"location":"configuration/","title":"Configuration","text":"<p>The normal config is <code>~/.bridgesessions/config</code>; override with <code>--config</code>.</p> <pre><code>node.name node-a\nnode.listen 192.0.2.10:19949\nmesh.require_seed_pins true\nmesh.mdns_enabled false\nmesh.reconnect_backoff_max_secs 300\nmesh.join_window_max_secs 300\nseed node-b 192.0.2.11:19949 pubkey=&lt;64-hex-ed25519-public-key&gt;\nsessions.default_shell /bin/bash -l\nsessions.persistence_path ~/.bridgesessions/sessions.json\nsessions.authorized_keys_path ~/.bridgesessions/authorized_keys\nsession.agent.command /bin/bash -lc 'exec hermes --tui'\nreceive_dir ~/.bridgesessions/received\ntransfer.max_bytes 8589934592\ntransfer.allow_sensitive_paths false\nfile.dest_allow_home false\n</code></pre> Directive Purpose <code>node.name</code>, <code>node.listen</code> node identity and mesh bind <code>seed ... pubkey=</code> pinned bootstrap/enrollment authority <code>mesh.require_seed_pins</code> reject unpinned seeds; keep enabled <code>mesh.ping_interval_secs</code>, <code>mesh.pong_timeout_secs</code> liveness <code>mesh.reconnect_backoff_max_secs</code> retry ceiling <code>mesh.join_window_max_secs</code> unknown-cert join window cap <code>sessions.default_shell</code> remote shell command <code>session.&lt;name&gt;.command</code> named-session command <code>sessions.authorized_keys_path</code> inbound trusted keys <code>receive_dir</code> inbox and default served-file root <code>transfer.max_bytes</code> per-file limit <code>transfer.allow_sensitive_paths</code> arbitrary/sensitive path access; high risk <p>Local daemon IPC uses loopback port 19980 plus an owner-only token. Isolated tests may set <code>BRIDGESESSIONS_IPC_PORT</code> consistently for daemon and CLI.</p> <p>See <code>config.example</code>.</p>"},{"location":"cua/","title":"Computer-use automation (CUA)","text":"<p><code>bs cua</code> captures and controls a trusted peer's interactive desktop through the BridgeSessions mesh.</p>"},{"location":"cua/#commands","title":"Commands","text":"<pre><code>bs cua screen &lt;peer&gt;\nbs cua capture &lt;peer&gt; -o screen.png\nbs cua capture &lt;peer&gt; -o screen.jpg --format 2 --quality 70\nbs cua move &lt;peer&gt; --x 500 --y 300\nbs cua click &lt;peer&gt; --x 500 --y 300 --button left\nbs cua type &lt;peer&gt; --text 'hello'\nbs cua key &lt;peer&gt; --code 40 --modifiers ctrl\nbs cua scroll &lt;peer&gt; --direction down --amount 5\nbs capture-video &lt;peer&gt; --duration 10 -o capture.mp4\n</code></pre> <p>Always capture first and confirm screen dimensions before clicking.</p>"},{"location":"cua/#architecture","title":"Architecture","text":"<pre><code>operator CLI \u2500\u2500 mutual TLS \u2500\u2500 peer daemon \u2500\u2500 token-auth loopback IPC \u2500\u2500 user-session helper\n</code></pre> <ul> <li>Linux: capture/input can run in the user's daemon session; common desktop tools may be required.</li> <li>Windows: the mesh daemon may run in Session 0. Start one <code>bridgesessions --cua-helper</code> in the logged-in user's session.</li> <li>macOS: run the helper in the logged-in user's session. Capture uses ScreenCaptureKit; input uses accessibility APIs.</li> </ul> <p>The helper listens only on loopback (default 19986) and authenticates every request with an owner-only random token under <code>~/.bridgesessions/</code>.</p>"},{"location":"cua/#setup","title":"Setup","text":""},{"location":"cua/#windows","title":"Windows","text":"<pre><code>bridgesessions --cua-helper\n</code></pre> <p>For persistent use, create an at-logon task in the interactive user's context. Do not run multiple helpers against one token file.</p>"},{"location":"cua/#macos","title":"macOS","text":"<pre><code>bridgesessions --cua-helper\n</code></pre> <p>Grant the installed, Developer ID-signed app/binary:</p> <ul> <li>Screen Recording (capture/video),</li> <li>Accessibility (mouse/keyboard).</li> </ul> <p>Restart the helper after changing permissions. Do not reset TCC during routine upgrades.</p>"},{"location":"cua/#hid-keys","title":"HID keys","text":"<p><code>cua key</code> uses USB HID keyboard usage IDs.</p> Key Decimal <code>a</code> 4 <code>s</code> 22 Enter 40 Escape 41 Backspace 42 Tab 43 Space 44 Right / Left / Down / Up 79 / 80 / 81 / 82 <p>Modifiers are comma-separated: <code>ctrl</code>, <code>shift</code>, <code>alt</code>, <code>meta</code>.</p>"},{"location":"cua/#security","title":"Security","text":"<ul> <li>Spectator attachments are rejected before any CUA action.</li> <li>Helper IPC is loopback-only and token-authenticated.</li> <li>Coordinates and payload sizes are validated.</li> <li>Capture/input results are returned to the requesting peer; the helper does not intentionally persist them.</li> <li>A trusted peer can still observe or control the desktop. CUA is not a low-privilege capability.</li> </ul>"},{"location":"cua/#troubleshooting","title":"Troubleshooting","text":"Symptom Check helper unavailable/auth mismatch stop duplicate helpers; restart one helper and the daemon black/empty macOS capture Screen Recording grant and helper restart macOS input ignored Accessibility grant Windows input hits wrong desktop helper must be in the intended interactive session wrong click target run <code>cua screen</code>, capture, then recalculate coordinates port 19986 busy terminate stale helper <p>For automated desktop verification, use E2E Framework.</p>"},{"location":"design/","title":"Design","text":"<p>BridgeSessions is one executable with three roles: mesh daemon, client CLI, and persistent session multiplexer.</p> <pre><code>CLI \u2500\u2500 mutual TLS \u2500\u2500 peer daemon \u2500\u2500 PTY / ConPTY\n                            \u251c\u2500\u2500 files\n                            \u251c\u2500\u2500 desktop helper\n                            \u2514\u2500\u2500 peer/session gossip\n</code></pre>"},{"location":"design/#core-decisions","title":"Core decisions","text":"<ul> <li>Pinned identity: each node owns an Ed25519 key. Trust is the raw pinned key, not a certificate subject or public CA.</li> <li>Persistent sessions: the server owns the terminal and child; attachments are temporary. Detach does not kill the session.</li> <li>Single event loop: established sockets are non-blocking and outgoing frames use bounded queues.</li> <li>Exclusive TLS ownership: long operations borrow one transport via <code>exec_busy</code>; no two threads touch the same <code>SSL*</code>.</li> <li>Verified transfers: metadata is bounded, paths canonicalized, writes use <code>.part</code>, SHA-256 precedes atomic publish, and valid partials survive reconnect.</li> <li>Backpressure: wire lengths, receive work per tick, TLS TX, PTY input, worker IPC, and logical output queues are bounded.</li> <li>Desktop isolation: Windows/macOS CUA uses a token-authenticated loopback helper in the interactive user session.</li> <li>Artifact separation: git contains source/automation; binaries and SBOMs are GitHub Release assets.</li> </ul>"},{"location":"design/#source-map","title":"Source map","text":"File Responsibility <code>main.cpp</code> CLI and self-upgrade <code>bs-protocol.h</code> codec, TLS, mesh, transfers, IPC <code>bs-session.h</code> session lifetime and scrollback <code>bs-session-worker.h</code> optional per-session process <code>bs-cua-helper.h</code> desktop helper <code>macos-capture.mm</code> ScreenCaptureKit"},{"location":"design/#current-limits","title":"Current limits","text":"<ul> <li><code>select()</code> inherits the platform FD ceiling; this targets small trusted fleets.</li> <li>Authorization is host-level, not capability-scoped.</li> <li>Cross-platform compatibility currently caps TLS at 1.2.</li> <li>Windows ConPTY and macOS permissions require platform E2E testing.</li> </ul>"},{"location":"protocol/","title":"Protocol (<code>bs://</code>)","text":"<p>BridgeSessions uses binary frames over mutually authenticated TLS/TCP.</p>"},{"location":"protocol/#transport","title":"Transport","text":"<ul> <li>TCP port: 19949 by default.</li> <li>Current compatibility profile: TLS 1.2.</li> <li>Identity: pinned Ed25519 public key carried in a self-signed certificate and repeated in Hello.</li> <li>Latency: <code>TCP_NODELAY</code> on interactive connections.</li> <li>Compression: zstd for payloads above 256 bytes.</li> </ul>"},{"location":"protocol/#frames","title":"Frames","text":"<p>Small/legacy frame:</p> <pre><code>stream_id:u16be | type:u8 | flags:u8 | length:u16be | payload\n</code></pre> <p>A peer advertising <code>+frm2</code> in <code>Hello.version</code> may use:</p> <pre><code>stream_id:u16be | type:u8 | flags:u8 | length:u32be | payload\n</code></pre> <p>Limits:</p> <ul> <li>u16 payload: 65,535 bytes,</li> <li><code>frm2</code> payload: 4 MiB,</li> <li>image input: 50 MiB before conversion/encoding,</li> <li>decompressed payload is checked against the same logical cap before allocation/use.</li> </ul> <p>Flags:</p> Bit Name Meaning <code>0x01</code> compressed zstd payload <code>0x02</code> control control stream frame <code>0x04</code> render markdown output rendering hint <code>0x08</code> u32 length 8-byte header / <code>frm2</code>"},{"location":"protocol/#streams","title":"Streams","text":"<ul> <li>Stream 0 is control: Hello, attach/detach, keepalive, gossip, transfer control.</li> <li>Non-zero streams identify session traffic on one connection.</li> <li>Stream IDs are connection-scoped. Reattach creates a new attachment; the server-side session persists.</li> </ul>"},{"location":"protocol/#message-families","title":"Message families","text":"Family Examples identity/mesh Hello, Gossip, DirectoryEnroll, Ping/Pong sessions Attach/Ack, Detach, SessionList, SessionDied, Signal terminal Keystroke, Output, Resize, Scrollback/Ack files FileMeta, FileChunk, FileAck, FileRequest clipboard Clipboard, ClipboardEcho desktop CuaRequest/Response, CuaVideoCapture/Result optional transports DHT and WebRTC negotiation messages <p>The authoritative enum, variant, serializers, and decoders are in <code>bs-protocol.h</code>. A new message is incomplete unless all four agree and round-trip tests cover it.</p>"},{"location":"protocol/#handshake-and-identity","title":"Handshake and identity","text":"<ol> <li>TCP connect.</li> <li>Mutual TLS handshake.</li> <li>Extract raw Ed25519 key from the peer certificate.</li> <li>Exchange Hello.</li> <li>Require the expected pin, certificate key, Hello key, and peer name to agree.</li> <li>Only then promote the transport to a live mesh connection.</li> </ol> <p>During a bounded invite window, an unknown certificate may send a JoinRequest. The single-use token is checked before authorization. Successful enrollment is signed; only configured seed keys are accepted as issuers.</p>"},{"location":"protocol/#file-transfer","title":"File transfer","text":"<ol> <li>Sender sends metadata: filename, byte size, SHA-256, chunk count, chunk size, optional destination.</li> <li>Receiver validates metadata and path, opens <code>&lt;target&gt;.part</code>, and replies with the first needed chunk (resume-aware).</li> <li>Sender streams a bounded window of chunks; receiver validates ordering/size and acknowledges progress.</li> <li>Receiver verifies SHA-256, atomically renames the partial, and returns a final ACK.</li> </ol> <p>Transport failure preserves a valid partial and sidecar for resume. Validation or write failure removes it.</p>"},{"location":"protocol/#backward-compatibility","title":"Backward compatibility","text":"<p>Capabilities are additive suffixes in <code>Hello.version</code> (for example <code>+frm2</code>). Never send a new frame shape to a peer that did not advertise it. Protocol changes require mixed-version tests, not only same-version loopback tests.</p>"},{"location":"usage/","title":"Usage","text":""},{"location":"usage/#mesh-and-shells","title":"Mesh and shells","text":"<pre><code>bs peers list\nbs health &lt;peer&gt;\nbs fleet\nbs stats\nbs shell &lt;peer&gt;\nbs shell &lt;peer&gt; --name agent\nbs shell &lt;peer&gt; --cmd 'hostname &amp;&amp; uptime'\nbs sessions &lt;peer&gt;\n</code></pre> <p>Named sessions survive disconnect. Use one stacked shell or <code>run-script</code> for dependent work; separate one-shots do not share state.</p>"},{"location":"usage/#files","title":"Files","text":"<pre><code>bs file send &lt;peer&gt; &lt;local&gt; --wait\nbs file send &lt;peer&gt; &lt;local&gt; --dest &lt;remote&gt; --wait\nbs file recv &lt;peer&gt; &lt;remote&gt; --to &lt;local&gt; --wait\nbs telemetry\n</code></pre> <p>Transfers are resumable and SHA-256 verified. Peers serve only from <code>receive_dir</code> by default.</p>"},{"location":"usage/#scripts","title":"Scripts","text":"<pre><code>bs run-script &lt;peer&gt; ./task.sh\nbs script add ./task.sh --name task\nbs script push task --peer &lt;peer&gt;\nbs script run task --peer &lt;peer&gt; -- 'arg with spaces'\n</code></pre> <p>Aliases are path-safe; cached scripts are content-addressed. Arguments are shell-quoted individually.</p>"},{"location":"usage/#computer-use","title":"Computer use","text":"<pre><code>bs cua screen &lt;peer&gt;\nbs cua capture &lt;peer&gt; -o screen.png\nbs cua click &lt;peer&gt; --x 500 --y 300\nbs cua type &lt;peer&gt; --text 'hello'\nbs capture-video &lt;peer&gt; --duration 10 -o capture.mp4\n</code></pre>"},{"location":"usage/#bootstrap-and-upgrade","title":"Bootstrap and upgrade","text":"<pre><code>bs invite\nbs join &lt;seed-address&gt;:19949 &lt;token&gt; --start\nbs upgrade\nbs upgrade --tag 26.09.19-beta6\n</code></pre> <p>Upgrade downloads GitHub Release assets and requires checksum/version verification.</p>"},{"location":"usage/#daemon","title":"Daemon","text":"<pre><code>bridgesessions --daemon --config ~/.bridgesessions/config\n</code></pre> <p>Platform installers generate service definitions. Do not copy obsolete templates from old tags.</p>"},{"location":"why-bridge-sessions/","title":"Why BridgeSessions","text":"<p>Remote work often combines a transport, a multiplexer, a file copier, platform-specific Windows automation, and ad hoc agent tooling. BridgeSessions puts the common path behind one peer identity and one CLI.</p> Need Traditional stack BridgeSessions encrypted shell SSH mutual-TLS mesh shell survive disconnect tmux/Zellij server-owned named PTY/ConPTY copy files SCP/SFTP resumable, hash-verified file protocol Windows automation WinRM/RDP helpers same peer mesh plus CUA helper agent artifacts paste/base64/chat uploads files, capture, and Bridge Panel"},{"location":"why-bridge-sessions/#the-useful-difference","title":"The useful difference","text":"<p>The network connection is not the session. The remote daemon owns the terminal and child process; clients attach and detach. A laptop sleep, Wi-Fi change, or client restart does not require restarting the shell or agent TUI.</p> <pre><code>bs shell build-peer --name agent\n# detach, reconnect later\nbs shell build-peer --name agent\n</code></pre> <p>Files and desktop captures use the same pinned peer identity:</p> <pre><code>bs file send build-peer ./artifact.bin --wait\nbs cua capture desktop-peer -o screen.png\n</code></pre>"},{"location":"why-bridge-sessions/#built-for-operator-controlled-meshes","title":"Built for operator-controlled meshes","text":"<ul> <li>One Ed25519 identity per node.</li> <li>Explicit seed pins and inbound <code>authorized_keys</code>.</li> <li>One TCP port for mesh traffic.</li> <li>Linux, macOS, and Windows peers.</li> <li>Structured progress/output that agents can parse.</li> </ul> <p>This is not a public multi-tenant remote-access service. Authorizing a peer is close to granting shell access; the security boundary is the peer key and the host account.</p>"},{"location":"why-bridge-sessions/#agent-workflow","title":"Agent workflow","text":"<p>A typical loop is:</p> <ol> <li>attach to a durable build/session peer,</li> <li>run or upload work,</li> <li>capture a remote desktop when GUI context matters,</li> <li>transfer the artifact,</li> <li>publish a concise report to Bridge Panel.</li> </ol> <p>Bridge Panel is optional and loopback-only by default. It is a document review surface, not a new trust root.</p>"},{"location":"why-bridge-sessions/#trade-offs","title":"Trade-offs","text":"<ul> <li>TCP/TLS favors deployability over MOSH-style UDP roaming.</li> <li><code>select()</code> and host-level authorization target small trusted fleets, not internet scale.</li> <li>CUA depends on user-session helpers and OS permissions.</li> <li>A compromised authorized node can affect other nodes that trust it.</li> <li>Beta releases require active upgrade discipline.</li> </ul> <p>See Design, Security, and Quickstart.</p>"}]}