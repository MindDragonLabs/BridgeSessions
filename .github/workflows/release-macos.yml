name: release-macos


on:
  workflow_dispatch:
  push:
    tags: ['v*']

concurrency:
  group: release-macos
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  probe:
    runs-on: macos-14
    steps:
      - name: minimal echo
        run: echo ok
